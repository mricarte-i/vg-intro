using UnityEngine;

public class RenderedBeat : MonoBehaviour
{
    public int BeatNumber;
    public float Position;

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
